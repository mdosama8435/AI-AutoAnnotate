version: '3.8'

services:
  web:
    build: .
    container_name: explainink-ai
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: unless-stopped
